Fog of War Chess smart contract for the Aztec network

uses multiparty computation techniques to enable two players to play a hidden information variant of chess without a trusted third party interpreting moves.

See `main.nr` for method definitions/descriptions and `index.ts` for a basic script that will initialise and play a game.

